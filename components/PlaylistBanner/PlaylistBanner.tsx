import React from "react";

type PlaylistBannerProps = {
  componentData?: any;
};

export const PlaylistBanner = ({ componentData }: PlaylistBannerProps) => {
  return <section className="playlist-banner">Playlist Banner</section>;
};

export default PlaylistBanner;
